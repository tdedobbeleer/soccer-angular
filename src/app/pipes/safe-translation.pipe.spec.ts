import {SafeTranslationPipe} from "./safe-translation.pipe";

describe('SafeTranslationPipe', () => {
    it('create an instance', () => {
        //const pipe = new SafeTranslationPipe();
        //expect(pipe).toBeTruthy();
    });
});
