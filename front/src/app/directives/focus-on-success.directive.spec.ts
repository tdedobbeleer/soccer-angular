import {FocusOnSuccessDirective} from "./focus-on-success.directive";

describe('FocusOnSuccessDirective', () => {
    it('should create an instance', () => {
        //const directive = new FocusOnSuccessDirective();
        //expect(directive).toBeTruthy();
    });
});
