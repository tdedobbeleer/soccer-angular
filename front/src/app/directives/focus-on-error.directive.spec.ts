import {FocusOnErrorDirective} from "./focus-on-error.directive";

describe('FocusOnErrorDirective', () => {
    it('should create an instance', () => {
        //const directive = new FocusOnErrorDirective();
        //expect(directive).toBeTruthy();
    });
});
